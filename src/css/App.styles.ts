import styled from 'styled-components';

export const RouteLoading = styled.main`
    flex: 1;
    padding: 8rem 1.5rem 4rem;
    text-align: center;
`;

export const AppWrapper = styled.div`
    display: flex;
    flex-direction: column;
    min-height: 100vh;
`;
